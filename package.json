{
  "scripts": {
    "format": "prettier --write .",
    "test": "jest --watch",
    "coverage": "jest --coverage"
  },
  "private": true,
  "devDependencies": {
    "jest": "^26.6.3",
    "prettier": "^2.2.1"
  }
}
