<?php
class ProjectCompletionStatusTest extends IntegrationTest {

    function testsCallOnValidProject() {
        $project = integrationCreateTestProject();

        // create api keys
        // create project owner
        // assign project completion feature to owner
        // set all segments translated
        // simulate click on 'send' button via curl
        $this->markTestIncomplete();
    }
}
