sudo: false
dist: bionic
language: node_js

node_js:
  - 14

cache: yarn

branches:
  only:
    - develop
addons:
  apt:
    update: true

install:
  - yarn install
  - nvm 9 && cd support_scripts/grunt && npm install
  - nvm 14

script:
  - yarn coverage
