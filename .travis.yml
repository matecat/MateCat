sudo: false
dist: bionic
language: node_js

node_js:
  - 9

cache: yarn

branches:
  only:
    - develop
addons:
  apt:
    update: true

install:
  - yarn install
  - cd support_scripts/grunt && npm install

script:
  - yarn coverage
