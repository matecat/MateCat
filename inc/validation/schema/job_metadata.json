{
  "$comment": "This schema gates writes only. Nothing gates reads per key: the editor's own read returns every key stored against a job to whoever opens it, which is by design, since the translator working a job is routinely outside the team that owns the project. A key added here is therefore readable by anyone who can open the job. Do not add one holding anything that audience should not see.",
  "type": "array",
  "items": {
    "anyOf": [
      {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "const": "character_counter_count_tags"
          },
          "value": {
            "type": [
              "boolean"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "const": "character_counter_mode"
          },
          "value": {
            "enum": [
              "google_ads",
              "exclude_cjk",
              "all_one"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "const": "subfiltering_handlers"
          },
          "value": {
            "$ref": "subfiltering_handlers.json"
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "const": "mandatory_issues"
          },
          "value": {
            "type": "array",
            "items": {
              "enum": ["r1", "r2"]
            }
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "const": "tm_prioritization"
          },
          "value": {
            "type": [
              "boolean"
            ]
          }
        }
      }
    ]
  }
}