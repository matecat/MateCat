{
    "$comment": "This schema gates writes only. Nothing gates reads per key: the editor's own read returns every key stored against a job to whoever opens it, which is by design, since the translator working a job is routinely outside the team that owns the project. A key added here is therefore readable by anyone who can open the job. Do not add one holding anything that audience should not see.",
  "type": "array",
  "items": {
    "anyOf": [
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "character_counter_count_tags"
          },
          "value": {
            "type": [
              "boolean"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "character_counter_mode"
          },
          "value": {
            "enum": [
              "google_ads",
              "exclude_cjk",
              "all_one"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "subfiltering_handlers"
          },
          "value": {
            "$ref": "subfiltering_handlers.json"
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "mandatory_issues"
          },
          "value": {
            "type": "array",
            "items": {
              "enum": ["r1", "r2"]
            }
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "tm_prioritization"
          },
          "value": {
            "type": [
              "boolean"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "mt_quality_value_in_editor"
          },
          "value": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "deepl_formality"
          },
          "value": {
            "enum": [
              "default",
              "prefer_less",
              "prefer_more"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "deepl_engine_type"
          },
          "value": {
            "enum": [
              "prefer_quality_optimized",
              "latency_optimized"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "deepl_id_glossary"
          },
          "value": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "lara_style"
          },
          "value": {
            "enum": [
              "faithful",
              "fluid",
              "creative"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "lara_style_guideline_id"
          },
          "value": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "lara_glossaries"
          },
          "value": {
            "type": "array",
            "$comment": "An empty list is an answer, not an absence: it is how a job says \"no glossary here\" over a project that has one, and the job scope wins. DELETE /metadata/{key} is the other operation — it drops the job row so the job inherits the project value again.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "mmt_glossaries"
          },
          "value": {
            "type": "array",
            "$comment": "An empty list is an answer, not an absence: it is how a job says \"no glossary here\" over a project that has one, and the job scope wins. DELETE /metadata/{key} is the other operation — it drops the job row so the job inherits the project value again.",
            "items": {
              "type": "integer",
              "minimum": 1
            }
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "value"
        ],
        "properties": {
          "key": {
            "const": "mmt_ignore_glossary_case"
          },
          "value": {
            "type": [
              "boolean"
            ]
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "const": "intento_provider"
          },
          "value": {
            "$comment": "Null is an answer, not an absence, and unlike `deepl_id_glossary` and `lara_style_guideline_id` this key cannot be cleared with DELETE /metadata/{key}. `intento_provider` and `intento_routing` are mutually exclusive and the provider outranks the routing, while the project row is written once at creation and never updated: dropping the job row would re-inherit the project's provider and silently discard the routing the job just chose. Null is stored as the empty string, the job scope's \"explicitly none\"; an empty string on the wire stays refused, exactly as it is for those two.",
            "type": [
              "string",
              "null"
            ],
            "minLength": 1,
            "maxLength": 255
          }
        }
      },
      {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "const": "intento_routing"
          },
          "value": {
            "$comment": "Null is an answer, not an absence, and unlike `deepl_id_glossary` and `lara_style_guideline_id` this key cannot be cleared with DELETE /metadata/{key}. `intento_provider` and `intento_routing` are mutually exclusive and the provider outranks the routing, while the project row is written once at creation and never updated: dropping the job row would re-inherit the project's provider and silently discard the routing the job just chose. Null is stored as the empty string, the job scope's \"explicitly none\"; an empty string on the wire stays refused, exactly as it is for those two.",
            "type": [
              "string",
              "null"
            ],
            "minLength": 1,
            "maxLength": 255
          }
        }
      }
    ]
  }
}
